{
  "schemaVersion": 1,
  "id": "accesodepepe",
  "version": "${version}",
  "name": "Acceso de Pepe",
  "description": "Al servidor de SOBRINOS DE PEPE se entra con el launcher: el mod le pide al cliente el permiso que emite el launcher y no deja entrar a quien no lo tenga.",
  "authors": ["SOBRINOS DE PEPE"],
  "environment": "*",
  "entrypoints": {
    "server": ["pe.sobrinosdepepe.acceso.AccesoServidor"],
    "client": ["pe.sobrinosdepepe.acceso.AccesoCliente"]
  },
  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": ">=26.1",
    "fabric-api": "*"
  }
}
